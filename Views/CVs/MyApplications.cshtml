@model List<talentacquisition_jobplacement_mvc.Models.CV>

@{
    ViewData["Title"] = "My Applications";
}

<h1>My Applications</h1>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show">
        @Html.Raw(TempData["Success"])
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (Model.Any())
{
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Position</th>
                <th>Applied On</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var cv in Model)
            {
                <tr>
                    <td><strong>@cv.Position?.Title</strong></td>
                    <td>@cv.CreatedAt.ToString("dd MMM yyyy HH:mm")</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@cv.Id" class="btn btn-sm btn-primary">View Details</a>
                        <a asp-action="Download" asp-route-id="@cv.Id" class="btn btn-sm btn-success">
                            <i class="bi bi-download"></i> Download PDF
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">
        You haven't applied to any positions yet.
        <a asp-controller="Positions" asp-action="Index" class="alert-link">Browse Positions →</a>
    </div>
}

<a asp-controller="Profiles" asp-action="Index" class="btn btn-secondary">← Back to Profile</a>